'use strict'

import URL from 'url-parse'
import cheerio from 'cheerio'



const crawler = {

    crawl: (domain, numLevels = 3,regexes = []) => {
        
    }
}


const visitPage = () => {
    console.log("im hungry")
}

export default crawler